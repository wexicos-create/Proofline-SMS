<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Removed: INTERNET and ACCESS_NETWORK_STATE are optional for offline messaging -->
    <!-- Only include if deep link features require network calls -->
    <!-- <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->

    <!-- Security-related permissions -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProoflineSMS"
        android:usesCleartextTraffic="false"
        tools:targetApi="35">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.compose.ComposeActivity"
            android:exported="false"
            android:launchMode="standard" />

        <!-- Deep Link Handler for secure message links -->
        <!-- Separate activity alias to avoid duplicate activity definitions -->
        <activity-alias
            android:name=".ui.main.SecureLinkHandler"
            android:exported="true"
            android:targetActivity=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Scheme: proofline://secure-link?message=<encrypted_data> -->
                <data
                    android:scheme="proofline"
                    android:host="secure-link"
                    android:pathPattern="/.*" />
            </intent-filter>
        </activity-alias>

        <!-- ContentProvider for secure data sharing (if needed) -->
        <!-- <provider
            android:name=".data.provider.ProoflineContentProvider"
            android:authorities="com.proofline.sms.provider"
            android:exported="false"
            android:permission="com.proofline.sms.READ_MESSAGES" /> -->

    </application>

</manifest>
